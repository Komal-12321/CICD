version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - pip install -r requirements.txt || echo "No requirements file"

  build:
    commands:version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo Installing dependencies...
      - pip install -r requirements.txt

  build:
    commands:
      - echo Build started
      - echo No build step required for Flask app

  post_build:
    commands:
      - echo Build completed successfully

artifacts:
  files:
    - '**/*'
      - echo Build started
      - python app.py || echo "No app file"

  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '*/'
